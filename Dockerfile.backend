FROM node:20-alpine
RUN apk add --no-cache docker-cli
WORKDIR /app
COPY backend/package*.json ./
RUN npm ci --omit=dev
COPY backend/ .
ARG APP_VERSION=dev
ENV APP_VERSION=$APP_VERSION
EXPOSE 5000
CMD ["node", "server.js"]
